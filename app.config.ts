export default defineAppConfig({
  ui: {
    // Theme defaults for Nuxt UI v4
    theme: {
      colors: ['primary', 'success', 'warning', 'error']
    },
    // Icon defaults can be extended here if needed
    // icons: { ... }
  }
})

